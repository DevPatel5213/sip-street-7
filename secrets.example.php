<?php
/**
 * TEMPLATE — do NOT put real values here.
 *
 * On the Hostinger server, create/edit  secrets.php  (NOT .example) in
 * public_html with your Brevo SMTP credentials:
 *
 *   <?php
 *   $SMTP_USER = 'your-brevo-login@email.com';   // Brevo → SMTP & API → SMTP → "Login"
 *   $SMTP_PASS = 'your-brevo-smtp-key';          // Brevo → SMTP & API → SMTP → SMTP key
 *
 * secrets.php is gitignored so it never reaches the public repo.
 */
$SMTP_USER = 'PUT-YOUR-BREVO-LOGIN-IN-secrets.php';
$SMTP_PASS = 'PUT-YOUR-BREVO-SMTP-KEY-IN-secrets.php';
