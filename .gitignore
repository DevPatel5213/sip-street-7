# OS / editor cruft
Thumbs.db
.DS_Store
*.log

# Local agent / preview config (not needed for the live site)
.claude/

# Original logo/photo drops — already copied into assets/
WhatsApp Image*.jpeg

# SECRET: SMTP mailbox password — must NEVER be committed (create only on the server)
secrets.php
